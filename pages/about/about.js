/**
 * Dondad Tech (Rector Hub) — About Page Logic
 */
(function () {
  'use strict';

  function ready(fn) {
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', fn);
    } else {
      fn();
    }
  }

  function initAboutPage() {
    // About page scripts
  }

  ready(initAboutPage);
})();
